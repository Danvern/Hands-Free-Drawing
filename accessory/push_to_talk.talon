mode: sleep
-
open (like|mike):
    user.toggle_key("alt")
    user.toggle_key("space")
    user.release_all_keys()
    # I need the proper toggle command that goes to sleep mode and then returns to pixel or command 